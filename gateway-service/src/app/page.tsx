export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>Compliance Gateway Service</h1>
      <p>
        API: <code>POST /api/chat</code> — Contract A in, Contract C SSE out.
      </p>
    </main>
  );
}
