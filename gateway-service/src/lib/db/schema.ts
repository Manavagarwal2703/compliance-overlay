/**
 * @deprecated
 * This mock in-memory store has been replaced by the Prisma + PostgreSQL
 * implementation in `src/lib/prisma.ts`.
 *
 * This file is intentionally left as a tombstone to surface a clear error
 * if any stale import paths still reference it.
 */

export {};
